# HTTP bootstrap only. install.sh replaces this with HTTPS configuration.
server {
    listen 80;
    server_name _;
    client_max_body_size 52m;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 503 "Complete HTTPS setup before logging in.\n"; }
}
