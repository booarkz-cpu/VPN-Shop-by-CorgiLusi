# Corgi CLI v15

Commands: `login`, `connect`, `disconnect`, `status`, `servers`, `diagnostics`, `devices`, `transfer`.
The CLI must use signed profiles and the same Corgi Identity API as mobile/desktop clients.
